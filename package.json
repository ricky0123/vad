{
  "name": "@ricky0123/vad",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many --target=build --skip-nx-cache",
    "test": "nx run-many --target=test --skip-nx-cache"
  },
  "private": false,
  "dependencies": {
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "nx": "15.7.1",
    "prettier": "^2.6.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
