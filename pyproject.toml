[tool.poetry]
name = "vad"
version = "0.1.0"
description = ""
authors = ["Ricky <rickycontact9@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
mkdocs = "^1.6.1"
mkdocs-material = "^9.5.42"
# Pinned: 1.2.3 added a dependency on "properdocs", a MkDocs fork, and prints a
# promo banner for it on every build. 1.2.2 needs only mkdocs.
mkdocs-redirects = "1.2.2"
pymdown-extensions = "^10.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
